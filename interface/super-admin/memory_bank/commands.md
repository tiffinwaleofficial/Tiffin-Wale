**6. Key build, run, test commands:**

*   `npm install` or `yarn install`
*   `npm run dev` or `yarn dev`
*   `npm run build`
*   `npm start`
*   `npm run lint`
*   `npm run typecheck`
*   `firebase emulators:start --only auth,firestore`
*   `gcloud app deploy`
*   `genkit:dev`
*   `genkit:watch`